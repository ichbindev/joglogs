import React from 'react';
// import bgimage from "";
// style={{ backgroundImage: `url(${bgimage})`, backgroundSize: 'cover' }}

const Hero = (props) => {
  return (
    <div className="heroSection4">
    <div className="heroSectionText4">
      <h1>Contact</h1>
      <h5>If you run, then you are part of the running community.  We love hearing from the community.  Shoot us a message</h5>
    </div>
  </div>
  );
};

export default Hero;