import React from 'react';
// import bgimage from "";
// style={{ backgroundImage: `url(${bgimage})`, backgroundSize: 'cover' }}

const HeroContact = (props) => {
  return (
    <div className="heroContact">
    <div className="heroContactText">
      <h1>Contact</h1>
      <h5>If you run, then you are part of the running community.  We love hearing from the community.  Shoot us a message</h5>
    </div>
  </div>
  );
};

export default HeroContact;